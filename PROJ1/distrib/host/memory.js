var TSOS;
(function (TSOS) {
    var memory = /** @class */ (function () {
        function memory() {
        }
        memory.prototype.init = function () {
        };
        return memory;
    }());
    TSOS.memory = memory;
})(TSOS || (TSOS = {}));
